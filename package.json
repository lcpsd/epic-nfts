{
  "name": "epic-nfts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "hardhat": "^2.7.0"
  },
  "scripts": {
    "hatbuild": "hardhat compile && hardhat test",
    "hattest": "yarn hardhat run scripts/run.js --network localhost",
    "hatserver": "yarn hardhat node",
    "hatrinkeby": "yarn hardhat run scripts/deploy.js --network rinkeby"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@openzeppelin/contracts": "^4.4.0",
    "chai": "^4.3.4",
    "dotenv": "^10.0.0",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.2"
  }
}
